#!/bin/sh

# Sets and samples the environment variables used by the release

# Tell Phoenix to start the server
export PHX_SERVER=true

# Database path
export DATABASE_PATH="${DATABASE_PATH:-/data/sqlite.db}"
